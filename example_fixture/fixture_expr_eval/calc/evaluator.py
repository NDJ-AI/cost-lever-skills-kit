"""Arithmetic expression evaluator.

Implement `evaluate` per the specification in TASK.md so that the tests in
tests/test_evaluator.py pass.
"""


def evaluate(expr: str) -> float:
    """Evaluate an arithmetic expression string and return the result as a float.

    See TASK.md for the full specification (operators, precedence, parentheses,
    unary minus, and error handling).
    """
    raise NotImplementedError("evaluate is not implemented yet")
